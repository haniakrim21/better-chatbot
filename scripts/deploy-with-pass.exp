#!/usr/bin/expect -f

set timeout -1
set password [lindex $argv 0]

spawn ./scripts/deploy-remote.sh

expect {
    "yes/no" {
        send "yes\r"
        exp_continue
    }
    "password:" {
        send "$password\r"
        exp_continue
    }
    eof
}
